# net-misc/rsync: 3.2.0 Fails to build on Alpine Linux
# https://github.com/WayneD/rsync/issues/29

diff --git a/configure.ac b/configure.ac
index 3d3bb6d..bfc3d41 100644
--- a/configure.ac
+++ b/configure.ac
@@ -230,6 +230,7 @@ __attribute__ ((target("ssse3"))) void more_testing(char* buf, int len)
 }
 ]])
 
+<<'###'
 if test x"$enable_simd" != x"no"; then
     # For x86-64 SIMD, g++ >=5 or clang++ >=7 is required
     if test x"$host_cpu" = x"x86_64"; then
@@ -255,6 +256,7 @@ Omit --enable-simd to continue without it.)
 Omit --enable-simd to continue without it.)
     fi
 fi
+###
 
 if test x"$SIMD" != x""; then
     AC_MSG_RESULT([yes ($SIMD)])
